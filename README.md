# Virtual column function
Adds a new column to a pandas DataFrame based on a simple arithmetic expression
between two existing columns.

Supports operators: +, -, *
Performs validation of column names and expression format.
Returns an empty DataFrame when validation fails.
